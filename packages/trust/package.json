{
  "name": "@verifyme/trust",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "vitest run"
  },
  "devDependencies": {
    "@verifyme/config": "workspace:*",
    "@verifyme/types": "workspace:*",
    "@verifyme/db": "workspace:*",
    "typescript": "^5.5.0"
  }
}
